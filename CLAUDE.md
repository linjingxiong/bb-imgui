# 项目协作原则（bb-imgui）

## 回答模版（强制）

**无论什么问题** —— 需求、bug、提问、确认、闲聊、"推送吗" —— **每一次回答都必须按下面三段式组织，没有例外：**

```
[当前问题]
说明现在的状态 / bug / 需求是什么，问题出在哪里。

[预期效果]
说明改完之后要达到的具体效果（视觉、交互、行为），越具体越好。

[计划]
列出要改的文件和步骤。
```

先按这个模版把 `[当前问题][预期效果][计划]` 讲清楚，等用户确认（"对" / "确认" / "可以" / "ok"）后**才能**动手（改代码 / 编译 / 截图核对 / 提交，全都要等确认）。

### 没有例外，指的是（这些全都要先走三段式再等确认）：

- 改动只有一两行、只是挪个坐标 / 调个数值 —— 也要。
- 用户上一条是命令语气（"往左移" / "字体改大" / "删掉这个"）—— 那是需求，不是"可以执行"。仍然先回三段式。
- 正在连续快速迭代、还在同一个话题的循环里 —— 每一条新消息都当成一个**全新请求**，重新走三段式。不存在"这是上一次已批准工作的延续"。
- 用户在纠正我上一次的理解 —— 也要三段式复述新理解再等确认。

### 每次回答前的自检（回答的第一件事）：

> 我这条回复是 `[当前问题] / [预期效果] / [计划]` 三段式吗？我是在"提出计划等确认"，还是已经"动手了"？如果已经动手 —— 停，改成三段式。

违反过很多次，根因是我在快速迭代时给自己找例外。上面把借口逐条堵死。

## 提交与推送分离

- 用户说"提交" → 只做本地 `git commit`。
- 只有用户明确说"推送"才 `git push`。
- commit 身份：`-c user.email=zsmelfry@gmail.com -c user.name=linjx`。

## "启动" = 运行 exe

用户说"启动"永远表示：编译（如有改动）并运行 `build/bin/Release/bb_imgui.exe`（带测试
mcap），**不是**推送、也不是别的。

## 其它

- 一律用中文回答。
- 不引入回归：新逻辑与既有路径隔离。
- 平台兼容：Windows / Linux 改动不能破坏另一端。
